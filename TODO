
* fixes/improvement
- fix ordering problem with list in lua table
- put time to/from frame conversion method in global function
- use time for refresh in anim and add a refresh by frame
- canvas must be itself a canvas_object
- write some doc ...

* new objects
- add `ListNamed (string * val_ext) list to val_ext type
- add a non-loop anim (anim_finished)
- add a sprite object handler with n graphic, n states and n properties
- add a sprite_stage to play with graphic
- add direct change of screen
